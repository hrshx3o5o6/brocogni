# PROMPT TO FEED INTO OPENCODE / CODEX / CLAUDE CODE FOR AUTONOMOUS DEPLOYMENT & HEALING

Copy and paste the prompt below directly into your coding agent (OpenCode, Claude Code, etc.) to instruct it to spawn subagents and solve any environment hurdles by editing the MCP server codebase:

---

## Task Instructions for the Agent

You are running inside a developer environment with full write and execution permissions over the current repository workspace (including `src/`, `examples/`, `package.json`, etc.). 

We are encountering a platform/environment limitation:
"Very large JSON lines or stdout payloads generated by the browser tools are being truncated by the client integration harness. This causes JSON parse errors when the truncated states are fed back as arguments into subsequent tools."

Do NOT treat the MCP server tools as a fixed, unchangeable environment. The MCP server code in this workspace is under your complete control. You can modify the server code to work around client-side limitations.

To resolve this and proceed with the original automation task, execute the following autonomous loop:

### 1. Spawn a Subagent to Workaround Caching/Truncation
Spawn a subagent to inspect the MCP server codebase (specifically `src/runtime/mcp.ts` and `src/runtime/contracts.ts`) and design/execute a code workaround.
* For example, the subagent should implement process-level state caching (storing `lastState` in memory) and make the `state` arguments optional across tools (like `browser_get_selector_plan`, `browser_verify`, `browser_act`, and `browser_delta`) so that you do not need to transmit massive JSON strings back and forth.
* If a tool like `browser_observe` is too large, the subagent should implement server-side search tools like `browser_find_targets` so you can retrieve only the specific nodes you need.

### 2. Spawn a Subagent to Build and Verify
Spawn a subagent to:
* Run the build command: `npm run build`
* Restart the MCP server process or tell me to reload the window.
* Run the integration tests (`npm test` and `node examples/test_mcp_client.js`) to ensure the server is responding correctly.

### 3. Iterate Until Verified
Keep running this loop:
1. Analyze the tool execution logs.
2. If truncation or state errors occur, spawn a subagent to adjust the caching or filter granularity of the MCP server.
3. Re-compile and verify.
4. Once verified, continue with the main browser automation task.

Do not give up or declare the platform limitation as "unfixable". Work around it programmatically by editing the codebase in this repository.
